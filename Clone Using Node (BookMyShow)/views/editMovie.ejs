<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex justify-center mt-3">
    <div>
        <a href="/" class="btn-link bg-blue-600 text-white px-3 py-2 rounded-md">Back</a>
        <a href="/movie" class="btn-link bg-blue-600 text-white px-3 py-2 rounded-md">Movie List</a>
        <div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-md">
            <h2 class="text-2xl font-semibold text-gray-800 mb-6 text-center">Edit Movie</h2>
            <form action="/movie/updateMovie/<%= movie.id %>" method="post" class="space-y-4" enctype="multipart/form-data">
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label for="Moviename" class="block text-sm font-medium text-gray-700">Movie Name</label>
                        <input type="text" id="Moviename" name="Moviename" value="<%= movie.Moviename %>"
                            class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 text-gray-700"
                            required>
                    </div>
                    <div>
                        <label for="actor" class="block text-sm font-medium text-gray-700">Actor</label>
                        <input type="text" id="actor" name="actor" value="<%= movie.actor %>"" placeholder=" Enter
                            actor's name"
                            class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 text-gray-700"
                            required>
                    </div>
                    <div>
                        <label for="releaseYear" class="block text-sm font-medium text-gray-700">Release Year</label>
                        <input type="number" id="releaseYear" name="releaseYear" value="<%= movie.releaseYear %>"
                            placeholder="Enter release year"
                            class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 text-gray-700"
                            required>
                    </div>
                    <div>
                        <label for="movietype" class="block text-sm font-medium text-gray-700">Movie Type</label>
                        <select id="movietype" name="movietype"
                            class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 text-gray-700">
                            <option value="Action" <%=movie.movietype==='Action' ? 'selected' : '' %>>Action</option>
                            <option value="Drama" <%=movie.movietype==='Drama' ? 'selected' : '' %>>Drama</option>
                            <option value="Comedy" <%=movie.movietype==='Comedy' ? 'selected' : '' %>>Comedy</option>
                            <option value="Horror" <%=movie.movietype==='Horror' ? 'selected' : '' %>>Horror</option>

                        </select>
                    </div>
                </div>
                <div>
                    <label for="movieImage" class="block text-sm font-medium text-gray-700">Movie Image</label>
                    <input type="file" id="movieImage" name="movieImage"
                        class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500 text-gray-700">
                    <img src="<%= movie.movieImage %>" height="100">
                </div>
                <div>
                    <button type="submit"
                        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                        edit
                    </button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>