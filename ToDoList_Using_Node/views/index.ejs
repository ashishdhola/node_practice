<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do-Using_node</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-purple-200">
    <div>
        <div>
            <form action="/addtask" method="post" class="bg-white rounded-lg shadow-lg w-96 mx-auto p-6 mt-12">
                <h1 class="text-2xl font-bold text-purple-700 mb-4 text-center">Enter Task</h1>
                <div class="flex gap-4">
                    <div class="flex-grow">
                        <input
                            type="text"
                            name="task"
                            class="task-input w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-400"
                            placeholder="Add Todo">
                    </div>
                    <div>
                        <input type="submit" class="btn bg-purple-600 text-white px-4 py-2 rounded-lg cursor-pointer hover:bg-purple-700" value="ADD">
                    </div>
                </div>
            </form>
        </div>
    </div>
    <!-- ========================================================alltask -->
    <div class="container max-w-lg mx-auto mt-8">
        <h1 class="completed-task text-lg font-bold text-center text-purple-800 mb-4">ALL TASK</h1>
        <div>
            <% alltask.map((val,id)=>{ %>
                <div class="task-data bg-gray-100 flex justify-between items-center rounded-lg px-4 py-3 mb-2">
                    <div class="task-detail left-data flex items-center">
                        <h6 class="text-white bg-purple-600 w-8 h-8 flex items-center justify-center rounded-full font-bold">
                            <%=id+1%>
                        </h6>
                        <h3 class="text-gray-800 font-medium ml-4">
                            <%= val.task %>
                        </h3>
                    </div>
                    <div>
                        <a href="/delete/<%=id%>" class="delete-btn bg-red-500 text-white px-3 py-1 rounded-lg hover:bg-red-600">Delete</a>
                        <a href="/complete/<%=id%>" class="complete-btn bg-green-500 text-white px-3 py-1 rounded-lg hover:bg-green-600">Mark as complete</a>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
    <!-- =========================================================completed task -->
    <div class="container max-w-lg mx-auto mt-8">
        <h1 class="completed-task text-lg font-bold text-center text-purple-800 mb-4">COMPLETED TASK</h1>
        <div>
            <% completedtask.map((val,id)=>{ %>
                <div class="task-data bg-gray-100 flex justify-between items-center rounded-lg px-4 py-3 mb-2">
                    <div class="task-detail left-data flex items-center">
                        <h6 class="text-white bg-purple-600 w-8 h-8 flex items-center justify-center rounded-full font-bold">
                            <%=id+1%>
                        </h6>
                        <h3 class="text-gray-800 font-medium ml-4">
                            <%= val.task %>
                        </h3>
                    </div>
                    <div>
                        ✅
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>
