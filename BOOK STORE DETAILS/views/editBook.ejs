<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book - Book Store</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-blue-50">
  <!-- Header -->
  <header class="w-full bg-blue-900 text-white py-5 shadow-lg">
    <div class="container mx-auto flex justify-between items-center px-6">
      <h1 class="text-3xl font-bold">📘 Book Store</h1>
      <a href="/" class="text-lg hover:underline transition">Home</a>
    </div>
  </header>

  <!-- Main Section -->
  <main class="py-12">
    <div class="max-w-4xl mx-auto bg-white shadow-2xl rounded-2xl p-10">
      <h2 class="text-3xl font-semibold text-blue-800 mb-8 text-center">Edit Book Details</h2>
      <form id="book-form" class="space-y-8" method="post" action="/books/editBook/<%= singleBook.id %>">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <!-- Book Name -->
          <div>
            <label for="book-name" class="block text-gray-600 font-medium">Book Name</label>
            <input type="text" value="<%= singleBook.bookName %>" name="bookName" placeholder="Enter book name"
              class="mt-3 w-full border border-gray-300 rounded-md p-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>

          <!-- Book Title -->
          <div>
            <label for="book-title" class="block text-gray-600 font-medium">Book Title</label>
            <input type="text" value="<%= singleBook.bookTitle %>" name="bookTitle" placeholder="Enter book title"
              class="mt-3 w-full border border-gray-300 rounded-md p-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>

          <!-- Author Name -->
          <div>
            <label for="author-name" class="block text-gray-600 font-medium">Author Name</label>
            <input type="text" value="<%= singleBook.authorName %>" name="authorName" placeholder="Enter author name"
              class="mt-3 w-full border border-gray-300 rounded-md p-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>

          <!-- Publish Date -->
          <div>
            <label for="publish-date" class="block text-gray-600 font-medium">Publish Date</label>
            <input type="date" value="<%= singleBook.publishDate %>" name="publishDate"
              class="mt-3 w-full border border-gray-300 rounded-md p-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>

          <!-- Book Price -->
          <div>
            <label for="book-price" class="block text-gray-600 font-medium">Price (in INR)</label>
            <input type="number" value="<%= singleBook.bookPrice %>" name="bookPrice" placeholder="Enter price"
              class="mt-3 w-full border border-gray-300 rounded-md p-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>

          <!-- Book Pages -->
          <div>
            <label for="book-pages" class="block text-gray-600 font-medium">Pages</label>
            <input type="number" value="<%= singleBook.bookPages %>" name="bookPages" placeholder="Enter pages"
              class="mt-3 w-full border border-gray-300 rounded-md p-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>

          <!-- Book Image URL -->
          <div class="md:col-span-2">
            <label for="book-image" class="block text-gray-600 font-medium">Book Image URL</label>
            <input type="url" value="<%= singleBook.bookImage %>" name="bookImage" placeholder="Enter image URL"
              class="mt-3 w-full border border-gray-300 rounded-md p-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>
        </div>

        <!-- Save Changes Button -->
        <div class="flex justify-end">
          <button type="submit"
            class="bg-blue-700 text-white py-3 px-8 rounded-lg font-bold hover:bg-blue-800 transition duration-300">
            Save Changes
          </button>
        </div>
      </form>
    </div>
  </main>
</body>

</html>
